// lambda function as parameter
fun pow(powOfTwo) {
    var powOfThree = fun (a) {
        print a * a * a;
    };
    for (var i = 1; i <= 3; i = i + 1) {
        powOfTwo(i);
        powOfThree(i);
    }
}

pow(fun (a) {
   print a * a;
});
